name: todo_app
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'


dependencies:
  flutter:
    sdk: flutter

  # responsive design
  flutter_screenutil: ^5.9.3
  # local storage
  hive_flutter: ^1.1.0
  # state management
  provider: ^6.0.5



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.2
  # for generating model classes and adapters like .g.dart files
  build_runner: ^2.4.11
  # for generating model register adapter
  hive_generator: ^2.0.1


flutter:
  uses-material-design: true
  generate: true
  